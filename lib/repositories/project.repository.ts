import "server-only"

// TODO (Sprint 3 – Phase 5): project persistence repository.
// Runtime queries go through the authenticated Supabase Server Client
// (lib/supabase/server.ts), not Drizzle. See lib/repositories/README.md.
//
// Planned methods:
// - createProject(input: { title: string; description?: string })
// - listProjects()
// - getProject(projectId: string)
// - deleteProject(projectId: string)

export {}
